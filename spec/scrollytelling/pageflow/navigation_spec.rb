require 'spec_helper'

describe Scrollytelling::Pageflow::Navigation do
  it 'has a version number' do
    expect(Scrollytelling::Pageflow::Navigation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
